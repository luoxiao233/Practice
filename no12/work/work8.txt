8.构造方法？特点？

构造方法主要是存在类中方法外。作用是初始化本类的对象。分别可以分为无参构造和有参构造


无参构造指的是创建一个自带默认值的对象


有参构造指的是创建一个与参数对应的属性的对象。



特点：  创建的时候方法名必须和类名一模一样，没有具体的返回值，也没有return关键字。
	例如
class Person{
	int a;
	public Person(){}    //无参构造
	public Person(int a){    //有参构造
		this.a = a; 
	}
}